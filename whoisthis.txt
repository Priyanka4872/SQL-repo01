USE JJBA;
DROP TABLE IF EXISTS EMPLOYEE;

CREATE TABLE EMPLOYEE(
EMP_ID INT PRIMARY KEY,
EMP_FNAME VARCHAR(20) NOT NULL,
EMP_LNAME VARCHAR(20),
DEPARTMENT VARCHAR(20) NOT NULL,
SALARY INT DEFAULT 20000 NOT NULL,
EMAIL VARCHAR(40)
);
INSERT INTO EMPLOYEE(EMP_ID,EMP_FNAME,EMP_LNAME,DEPARTMENT,SALARY,EMAIL) 
 VALUES
 (1001,"ALEX","D","TESTING",25000,"alex@compony.com"),
 (1002,"Celine","K","ACCOUNT",16500,"celine@compony.com"),
 (1003,"JOHN","L","SDE",45000,"johnx@compony.com"),
 (1004,"SELENA","D","MARKETING",22500,"selena@compony.com"),
 (1005,"ENRIQUE","I","IT",35000,"enrique@compony.com");
 SELECT * FROM EMPLOYEE;

SELECT*FROM EMPLOYEE WHERE UPPER(EMP_FNAME)='CELINE';
SELECT*FROM EMPLOYEE WHERE LOWER(EMP_FNAME)='Celine';
SELECT*FROM EMPLOYEE WHERE UPPER(DEPARTMENT) LIKE "%G";
SELECT DISTINCT DEPARTMENT FROM EMPLOYEE;
SELECT * FROM EMPLOYEE WHERE UPPER(DEPARTMENT) IN ("TESTING","SDE");
SELECT * FROM EMPLOYEE WHERE UPPER(DEPARTMENT) NOT IN ("TESTING","SDE");
SELECT  * FROM EMPLOYEE WHERE TRIM(UPPER(DEPARTMENT))="NETWORKING";
SELECT * FROM EMPLOYEE WHERE UPPER(EMP_FNAME) LIKE "E%" AND DEPARTMENT IN ("IT");


SELECT 
 *,
 CASE WHEN SALARY>30000 THEN"SENIOR"
 ELSE "JUNIOR"
 END AS GRADE
 FROM EMPLOYEE;


 SELECT 
 *,
 CASE WHEN SALARY>30000 THEN "SENIOR"
WHEN  SALARY>20000 AND SALARY<30000 THEN "MID-SENIOR"
 ELSE "JUNIOR"
 END AS GRADE
 FROM EMPLOYEE;


SELECT 
 *,
 IF(SALARY>30000,"SENIOR",
        IF(SALARY>20000 AND SALARY<30000,"MID-SENIOR","JUNIOR"))
 AS GRADE
 FROM EMPLOYEE;