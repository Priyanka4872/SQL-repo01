ALTER TABLE SANDBOX_TEACHER ADD CONSTRAINT FK_TEACHER_DEPARTMENT FOREIGN KEY(DEPT_ID) REFERENCES SANDBOX_DEPARTMENT(DEPT_ID);




USE JJBA;
DROP TABLE IF EXISTS DEPARTMENT;
DROP TABLE IF EXISTS TEACHER;
DROP TABLE IF EXISTS SUBJECT;
DROP TABLE IF EXISTS CLASSROOM;

CREATE TABLE DEPARTMENT(
DEPT_ID VARCHAR(10) PRIMARY KEY,
DEPT_NAME VARCHAR(10),
BUDGET INT,
BULDING VARCHAR(10)
);

CREATE TABLE TEACHER(
TEACHER_ID VARCHAR(10) PRIMARY KEY,
TEACHER_NAME VARCHAR(15),
DEPT_ID VARCHAR(10),
SALARY INT,
HIREDATE date
);

CREATE TABLE SUBJECT(
SUB_ID VARCHAR(10) PRIMARY KEY,
SUB_NAME VARCHAR(10),
DEPT_ID VARCHAR(10),
TEACHER_ID VARCHAR(10),
CREDITS INT
);

CREATE TABLE CLASSROOM(
CLASS_ID VARCHAR(10) PRIMARY KEY,
ROOM_NNAME VARCHAR(10),
SUB_ID VARCHAR(10),
CAPACITY INT,
HAS_PROJECTOR BOOLEAN
);

CREATE TABLE TEACHER (
TEACHER_ID VARCHAR(10) PRIMARY KEY,
TEACHER_NAME VARCHAR(15),
FOREIGN KEY(DEPT_ID) REFERENCES DEPARTMENT(DEPT_ID),
SALARY INT,
HIREDATE DATE
);

CREATE TABLE TEACHER (
TEACHER_ID VARCHAR(10) PRIMARY KEY,
TEACHER_NAME VARCHAR(15),
DEPT_ID VARCHAR(10),
FOREIGN KEY(DEPT_ID) REFERENCES DEPARTMENT(DEPT_ID),
SALARY INT,
HIREDATE DATE
);

CREATE TABLE SUBJECT(
SUB_ID VARCHAR(10) PRIMARY KEY,
SUB_NAME VARCHAR(10),
DEPT_ID VARCHAR(10),
FOREIGN KEY(DEPT_ID) REFERENCES DEPARTMENT(DEPT_ID),
TEACHER_ID VARCHAR(10),
FOREIGN KEY(TEACHER_ID) REFERENCES TEACHER(TEACHER_ID),
CREDITS INT
);

CREATE TABLE CLASSROOM(
CLASS_ID VARCHAR(10) PRIMARY KEY,
ROOM_NNAME VARCHAR(10),
SUB_ID VARCHAR(10),
FOREIGN KEY(SUB_ID) REFERENCES SUBJECT(SUB_ID),
CAPACITY INT,
HAS_PROJECTOR BOOLEAN
);

INSERT INTO Departments (DepartmentID, DepartmentName, Budget, Building) VALUES
('D1', 'Computer Science', 500000, 'Turing Hall'),
('D2', 'Mathematics', 250000, 'Gauss Tower'),
('D3', 'Physics', 300000, 'Einstein Lab'),
('D4', 'History', 120000, 'Lincoln Wing');


